/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Removed 'standalone' output for development
  // This allows hot reloading and development features
};

export default nextConfig;
